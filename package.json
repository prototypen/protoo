{
  "name": "protoo",
  "description": "A new Personal Publishing Platform that gives the user the most options to publish common types of media without being in the way.",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "express": "~3.0.3",
    "jade": "*",
    "stylus": "0.30.x",
    "coffee-script": "1.4.x",
    "request": "2.11.x",
    "jasmine-node": "1.x",
    "async": "0.1.x",
    "mongoose": "3.4.x",
    // TODO: Change to master branch if fixed
    "everyauth": "git://github.com/bnoguchi/everyauth.git#express3"
  },
  "scripts": {
    "start": "node app.js",
    "test": "jasmine-node --color --coffee spec",
    "autotest": "jasmine-node --color --coffee --autotest spec"
  }
}
